import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'damm.rodrigo.jimena.mario',
  appName: 'Sightseek',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
